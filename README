# Blocks.js

```js
var block = new Block('path/to/module',{
  children: {
    header: new Block('path/to/header')
    footer: new Block('path/to/footer')
  }
});
document.getElementById('container').appendChild(block.toElement());
```
